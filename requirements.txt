# Training (GPU side, GCP Vertex AI Workbench L4)
# Pin majors to keep the QLoRA recipe reproducible.
# Pinned to versions where Whisper QLoRA training is known to work:
# 4.49 + peft 0.15 raises 'WhisperForConditionalGeneration.forward() got
# unexpected keyword argument input_ids' even with the data-collator fix.
transformers==4.46.3
datasets>=3.0,<4.0
accelerate>=1.0,<2.0
peft==0.13.2
bitsandbytes>=0.45,<0.50
evaluate>=0.4
jiwer>=3.0
librosa>=0.10
soundfile>=0.12
audiomentations>=0.36
wandb>=0.18
scipy>=1.13
pandas>=2.2
numpy>=1.26,<2.2
tqdm>=4.66
psutil>=6.0
PyYAML>=6.0

# Conversion + faster-whisper inference
ctranslate2>=4.4
faster-whisper>=1.0.3

# Optional GPU-only deps (skip on CPU benchmark image):
# flash-attn==2.6.3       ; install with: pip install flash-attn --no-build-isolation
